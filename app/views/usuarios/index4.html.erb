<h1>Listado de Usuarios (Búsqueda)</h1>

<% form_tag busqueda_path, :method => :'get' do %>
 <p>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag "Buscar", :nombre => 'nil' %>
 </p>
<% end %>

Resultado:
<table border=1>
  <tr>
    <th>Nombre</th><th>Rut</th><th>Edad</th><th>Email</th>
  </tr>
<% @resp.each do |usuario| %>
  <tr>
    <td><%= usuario.nombre %></td>
    <td><%= usuario.rut %>-<%= usuario.digito %></td>
    <td><%= usuario.edad %></td>
    <td><%= usuario.email %></td>
  </tr>
<% end %>
</table>
<p><hr></p>

<h1>Listado completo de Usuarios</h1>

<table border=1>
  <tr>
    <th>Nombre</th><th>Rut</th><th>Edad</th><th>Email</th>
    <th colspan=3></th>
  </tr>
  <% @usuarios.each do |usuario| %>
  <tr>
    <td><%= usuario.nombre %></td>
    <td><%= usuario.rut %>-<%= usuario.digito %></td>
    <td><%= usuario.edad %></td>
    <td><%= usuario.email %></td>
    <td><%= link_to 'Show', usuario %></td>
    <td><%= link_to 'Edit', edit_usuario_path(usuario) %></td>
    <td><%= link_to 'Destroy', usuario, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br><%= link_to 'Nuevo Usuario', new_usuario_path %>

